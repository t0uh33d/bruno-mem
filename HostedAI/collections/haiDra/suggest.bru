meta {
  name: suggest
  type: http
  seq: 7
}

post {
  url: http://localhost:8000/suggest
  body: json
  auth: inherit
}

body:json {
  {
      "pool_name": "{{pool_name}}",
      "num_vgpus": "{{num_vgpus}}",
  }
}

settings {
  encodeUrl: true
}
