meta {
  name: delete-service
  type: http
  seq: 10
}

post {
  url: http://localhost:8000/delete-service
  body: json
  auth: inherit
}

body:json {
  {
      "service_name": "{{service_name}}",
      "team_id": "1820abf7-1e5d-4391-bc25-99021c8a3e98"
  }
}

script:post-response {
  const response = res.getBody().operation_id;
  bru.setEnvVar('operation_id', response);
}

settings {
  encodeUrl: true
}
