meta {
  name: operation status
  type: http
  seq: 12
}

post {
  url: http://localhost:8000/expose-service/status
  body: json
  auth: inherit
}

body:json {
  {
      "operation_id" : "{{operation_id}}",
      "service_name" : "{{service_name}}",
      "team_id" : "{{team_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
