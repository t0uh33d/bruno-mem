meta {
  name: get service perview
  type: http
  seq: 10
}

get {
  url: {{url}}/service/{{service_id}}/preview
  body: json
  auth: bearer
}

params:query {
  ~sort: created_at
  ~order: desc
  ~name[eqstr]: cPanel
  ~service_type[eqstr]: cpu_gpu_card
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  // // const response = pm.response.text()['token'];
  // // pm.environment.set('token', response);
}

settings {
  encodeUrl: true
}
