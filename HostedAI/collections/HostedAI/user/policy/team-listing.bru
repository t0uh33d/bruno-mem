meta {
  name: team-listing
  type: http
  seq: 6
}

get {
  url: {{url}}/policy/{{policy_type}}/team-listing
  body: none
  auth: bearer
}

params:query {
  ~sort: name
  ~order: asc
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
