meta {
  name: login
  type: http
  seq: 6
}

post {
  url: {{adminURL}}/login
  body: json
  auth: inherit
}

body:json {
  {
     "username" : "hosted_ai",
     "password":"{{adminPassword}}"
   }
  
  // {
  //     "email" :"sandy@codewave.com",
  //     "password":"Sandip@123"
  // }
}

script:post-response {
  const response = res.getBody().token;
  bru.setEnvVar('adminToken', response);
  
}

settings {
  encodeUrl: true
}
